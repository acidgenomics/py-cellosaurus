[build-system]
build-backend = "uv_build"
requires = ["uv_build>=0.11,<1"]

[project]
name = "cellosaurus"
version = "0.2.0"
description = "Cellosaurus identifier mapping toolkit."
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.12"
authors = [{ name = "Michael Steinbaugh", email = "mike@steinbaugh.com" }]
maintainers = [{ name = "Acid Genomics" }]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Science/Research",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Scientific/Engineering :: Bio-Informatics",
  "Typing :: Typed",
]
dependencies = ["pandas"]

[project.optional-dependencies]
docs = ["myst-parser", "numpydoc", "pydata-sphinx-theme", "sphinx"]
develop = [
  "bumpver",
  "coverage",
  "interrogate",
  "pyright[nodejs]",
  "pytest",
  "pytest-cov",
  "ruff",
  "ty",
  "uv",
]

[project.urls]
Homepage = "https://github.com/acidgenomics/py-cellosaurus"
Documentation = "https://python.acidgenomics.com/cellosaurus/"
Repository = "https://github.com/acidgenomics/py-cellosaurus"
Changelog = "https://github.com/acidgenomics/py-cellosaurus/blob/main/CHANGELOG.md"

[tool.bumpver]
commit = true
commit_message = "Bump to {new_version}."
current_version = "v0.2.0"
push = false
tag = false
tag_message = "{new_version}"
tag_scope = "default"
version_pattern = "vMAJOR.MINOR.PATCH"

[tool.bumpver.file_patterns]
"pyproject.toml" = ['current_version = "{version}"', 'version = "{pep440_version}"']

[tool.coverage.run]
relative_files = true
source = ["src/cellosaurus"]

[tool.interrogate]
fail-under = 80
ignore-init-method = true
ignore-init-module = true
ignore-property-decorators = false
ignore-setters = false
verbose = 2

[tool.numpydoc_validation]
checks = [
  "all",  # report on all checks, except
  "GL01", # do not require docstring formatting that conflicts with ruff
  "GL02", # ...
  "GL03", # ...
  "GL08", # nor require __init__ docstrings (params documented in class docstring)
  "ES01", # nor require an extended summary
  "EX01", # nor examples
  "PR02", # nor require parameter docs for dataclasses (fields are self-documenting)
  "SA01", # nor a see also section
  "SS06", # nor require the summary to fit on one line
]
# Allow docstrings to start with these words.
override_SS05 = ['^Access ', '^Assess ', '^Process ']
# Skip private (underscore-prefixed) functions/methods: node_name is dotted
# module-qualified, so match the underscore after the final dot rather than
# anchoring on ^_.
exclude = ['\._\w']

[tool.pyright]
pythonVersion = "3.12"
verboseOutput = true
venvPath = "."
venv = ".venv"
exclude = ["tests", ".venv"]

[tool.pytest.ini_options]
testpaths = ["src", "tests"]
addopts = "-v --tb=short --doctest-modules"
pythonpath = ["src"]
markers = ["network: marks tests that require network access"]

[tool.ruff]
indent-width = 4
line-length = 100
target-version = "py312"

[tool.ruff.format]
docstring-code-format = true
docstring-code-line-length = 72
quote-style = "double"

[tool.ruff.lint]
# List of rules: https://docs.astral.sh/ruff/rules/
ignore = [
  "D203",   # one-blank-line-before-class
  "D213",   # multi-line-summary-second-line
  "D401",   # non-imperative-mood
]
per-file-ignores = { "src/cellosaurus/_parse.py" = [
  "PLC0415", # intentional lazy imports (tempfile, importlib.metadata)
], "tests/**" = [
  "ANN001",
  "ANN201",
  "ANN202",
  "D101",
  "D102",
  "D103",
  "D104",
  "F841",
  "N803",
  "PLC0415",
  "PLR2004",
] }
select = [
  "ANN",  # flake8-annotations
  "B",    # flake8-bugbear
  "C90",  # mccabe
  "D",    # pydocstyle
  "E",    # pycodestyle
  "F",    # PyFlakes
  "FIX",  # flake8-fixme
  "I",    # isort
  "N",    # pep8-naming
  "NPY",  # NumPy-specific rules
  "PL",   # pylint
  "PT",   # flake8-pytest-style
  "RUF",  # Ruff-specific rules
  "SIM",  # flake8-simplify
  "SLOT", # flake8-slots
  "UP",   # pyupgrade
  "W",    # pycodestyle
]

[tool.ruff.lint.isort]
extra-standard-library = ["typing"]
known-first-party = []
order-by-type = true
relative-imports-order = "closest-to-furthest"
section-order = [
  "future",
  "standard-library",
  "third-party",
  "first-party",
  "local-folder",
]

[tool.ruff.lint.mccabe]
max-complexity = 30

[tool.ruff.lint.pycodestyle]
max-doc-length = 100

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.ruff.lint.pylint]
allow-magic-value-types = ["bytes", "int", "str"]
max-args = 30
max-branches = 30
max-positional-args = 3
max-statements = 100

[tool.ty]

[tool.ty.environment]
python-version = "3.12"
python = ".venv"

[tool.ty.src]
exclude = [".venv", "**/__pycache__", "**/.*", "tests"]

[tool.ty.terminal]
error-on-warning = true

[tool.uv]
package = true

[tool.uv.build-backend]
module-name = "cellosaurus"
module-root = "src"
